export const shopNotifierCss = `
  .shop-notifier-section {
    display: grid;
    gap: 12px;
  }

  .shop-card {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 12px;
  }

  .settings-card {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 12px;
  }
`;
