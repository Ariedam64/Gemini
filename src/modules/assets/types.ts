// src/modules/assets/types.ts
// Type definitions for MGAssets module

/**
 * Options for base URL initialization
 */
export interface AssetsInitOptions {
  /** Custom game version (defaults to auto-detected version) */
  gameVersion?: string;
}
